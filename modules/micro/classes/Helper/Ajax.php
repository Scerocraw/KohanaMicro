<?php

/**
 * 
 */
class Micro_Ajax {

    /**
     * AJAX Routing
     * @param type $uri
     * @param type $uriParts
     * @param type $postData
     */
    public static function route($uri, $uriParts, $postData) {
        // jsonResponse
        $jsonResponse = false;
        
        // Handling
        switch ($uri) {

        }
        
        // Return the response
        echo json_encode($jsonResponse);die();
    }

}
